apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: grafana-dashboards
    files:
      - dashboards/pod-logs.json
    labels:
      grafana_dashboard: "1"
resources:
  - namespace.yaml
  - helm-release.yaml
  - loki-datasource.yaml

